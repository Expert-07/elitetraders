<!DOCTYPE html>
<html>
<head>
  <title>Choose Payment Method</title>
  <style>
    body { font-family: sans-serif; padding: 30px; background: #f9f9f9; }
    .container { background: #fff; padding: 20px; border-radius: 10px; max-width: 500px; margin: auto; box-shadow: 0 0 10px #ccc; }
    button {
      display: block;
      width: 100%;
      margin: 10px 0;
      padding: 12px;
      font-size: 16px;
      background: #009879;
      color: white;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    button:hover { background: #007f6a; }
  </style>
</head>
<body>
  <div class="container">
    <h2>Select Payment Method</h2>
    <p>Plan: <%= investment.plan %></p>
    <p>Amount: ₦<%= investment.amount %></p>

    <form action="/payment/<%= investment.id %>" method="GET">
      <button type="submit" name="method" value="usdt">Pay with USDT (TRC20)</button>
      <button type="submit" name="method" value="card">Pay with Card</button>
      <button type="submit" name="method" value="bitcoin">Pay with Bitcoin</button>
    </form>
  </div>
</body>
</html>